# Feature Specification: ModifyModelNameTool

**Feature Name**: 设备型号伪装与重置工具
**Short Name**: model-name-tool
**Created**: 2026-03-08
**Status**: Draft

---

## Overview

一款桌面端 GUI 工具，用于通过 ADB 连接 Android 设备后，读取/修改/还原设备的型号信息（brand、manufacturer、model）。工具支持伪装设备信息的本地数据库管理，方便测试人员快速切换设备身份以进行高帧游戏适配测试。

### Problem Statement

测试人员在进行游戏性能优化时，需要频繁将设备伪装为不同厂商型号，以触发游戏对特定机型的高帧率策略。当前依赖手动执行 adb 命令完成伪装和还原，操作繁琐且易出错。

### Target Users

- 游戏性能优化测试工程师
- OEM 厂商游戏策略调试人员

---

## User Scenarios & Testing

### Scenario 1: 连接设备并查看当前信息

**前置条件**: 工具已启动，Android 设备通过 USB 连接 PC

1. 用户启动工具
2. 工具自动检测 ADB 设备连接
3. 第一行「当前设备信息」自动显示设备的 brand、manufacturer、model
4. 右侧显示当前设备是否处于「已伪装」或「未伪装」状态
5. 设备插拔时自动刷新第一行信息和按钮状态

**验收标准**:

- 设备连接后 3 秒内自动显示设备信息
- 未连接设备时显示提示状态，操作按钮置灰不可点击
- 设备拔出后自动清空第一行信息并恢复未连接状态
- 伪装状态准确反映当前设备属性是否与 vendor 分区原始值一致

### Scenario 2: 手动输入伪装信息并执行

**前置条件**: 设备已连接

1. 用户在第二行「伪装设备信息」的 brand、manufacturer、model 输入框中填写目标值
2. 用户点击 **Start**
3. 工具校验三个输入框是否全部填写，若有空字段则弹出提示要求补全
4. 工具检查输入值是否与当前设备信息完全一致
   - 若一致：弹窗提示「当前设备信息与待伪装设备信息一致，无需执行伪装操作」，不执行
5. 工具检查数据库中是否已存在当前 brand+manufacturer+model 组合
   - 若不存在：弹出保存对话框（见 Scenario 4）
   - 若已存在：直接执行伪装
5. 第三行进度区域依次显示：
   - "设备信息修改中......"
   - "正在重启设备请稍后......"
   - "设备信息伪装成功：brand=XX, manufacturer=XX, model=XX"
6. 第一行更新为伪装后的设备信息，状态显示「已伪装」

**异常流程**:

- adb root 失败：进度区域打印错误信息，提示用户使用 root 版本设备
- adb 环境不可用：进度区域打印错误信息，提示用户配置 adb 环境
- 其他 adb 命令失败：进度区域打印具体错误信息并终止流程

**验收标准**:

- 输入框未全部填写时点击 Start 弹出补全提示，不执行操作
- 输入值与当前设备信息一致时弹窗提示，不执行操作
- 进度信息实时更新，反映真实执行阶段
- 伪装成功后通过 `getprop` 验证属性值正确
- 设备重启完成后自动刷新第一行信息
- 操作失败时进度区域显示有针对性的错误提示

### Scenario 3: 从数据库选取设备信息

**前置条件**: 数据库中已有保存的设备信息

1. 用户点击「伪装设备信息:」后的快捷选取按钮
2. 弹出固定大小的弹窗，位置在按钮右下角
3. 左侧显示已保存的 model 列表
4. 鼠标悬停在某个 model 上时，右侧实时显示该条记录的备注内容
5. 用户点击选中某条记录
6. 弹窗关闭，brand、manufacturer、model 三个输入框自动填充

**验收标准**:

- 弹窗位置固定在按钮右下角
- 悬停时实时显示备注
- 选中后三个字段全部自动填充

### Scenario 4: 保存新设备信息到数据库

**前置条件**: 用户点击 Start 且当前组合不在数据库中

1. 弹出保存对话框，包含：
   - brand 输入框（已预填当前值）
   - manufacturer 输入框（已预填当前值）
   - model 输入框（已预填当前值）
   - 备注输入框（置灰提示文字："请描述此设备信息对应哪些高帧游戏"）
2. 用户填写备注信息
3. 用户点击 **Save** 保存，或点击 **Cancel** 取消
4. 保存成功后继续执行伪装流程

**验收标准**:

- 对话框中 brand/manufacturer/model 预填当前输入值
- 备注输入框在未输入时显示灰色提示文字
- Save 后数据持久化到本地数据库
- Cancel 后不保存且不执行伪装

### Scenario 5: 输入框自动补全与历史记忆

**前置条件**: 数据库中已有保存的设备信息

1. 用户在 brand/manufacturer/model 任一输入框中输入内容
2. 下拉箭头显示匹配的历史输入记录
3. 用户选取三项中的任一项后，其余两项自动从数据库中带出填入
4. 如有多条匹配记录，按首字母顺序排序，自动填入第一条

**验收标准**:

- 历史记录按输入内容过滤
- 选取单项后其余两项自动填充
- 多条匹配时按首字母排序取第一条

### Scenario 6: 清除伪装输入

1. 用户点击 **Clear**
2. 第二行的 brand、manufacturer、model 输入框全部清空

**验收标准**:

- 仅清除输入框内容，不影响设备状态
- 不清除进度区域内容

### Scenario 7: 重置设备为原始信息

**前置条件**: 设备当前处于伪装状态

1. 用户点击 **Reset**
2. 第三行进度区域依次显示：
   - "设备信息修改中......"
   - "正在重启设备请稍后......"
   - "设备信息重置成功：brand=XX, manufacturer=XX, model=XX"
3. 设备恢复为 vendor 分区的原始属性值
4. 第一行更新，状态显示「未伪装」

**异常流程**: 同 Scenario 2 的异常处理

**验收标准**:

- 原始值从 vendor 分区自动读取
- 重置后 `getprop` 验证属性恢复正确
- 状态标识更新为「未伪装」

### Scenario 8: 编辑已保存的设备档案

**前置条件**: 数据库中已有保存的设备信息

1. 用户在设备选取弹窗中右键点击某条记录，选择「编辑」
2. 弹出编辑对话框，格式与保存对话框一致，预填当前记录值
3. 用户修改字段后点击 Save 保存
4. 数据库记录更新

**验收标准**:

- 编辑后数据库中对应记录更新
- 若修改了 brand+manufacturer+model 组合且与已有记录冲突，提示用户

### Scenario 9: 删除已保存的设备档案

**前置条件**: 数据库中已有保存的设备信息

1. 用户在设备选取弹窗中右键点击某条记录，选择「删除」
2. 弹出确认对话框，用户确认后删除
3. 弹窗列表刷新

**验收标准**:

- 删除后数据库中对应记录移除
- 列表实时刷新

### Scenario 10: 导入设备档案数据

1. 用户通过菜单或按钮选择导入功能
2. 选择 JSON 格式的设备档案文件
3. 工具解析文件并将记录导入数据库
4. 与已有记录重复的条目跳过，仅导入新条目

**验收标准**:

- 支持标准 JSON 格式的设备档案文件
- 导入完成后显示导入成功条数和跳过条数
- 数据库中不产生重复记录

---

## Functional Requirements

### FR-1: 设备连接检测与持续监听

- 工具启动后自动检测 ADB 设备连接状态
- 持续监听设备插拔事件，设备插入时自动读取并显示信息，拔出时自动清空并更新状态
- 设备连接时自动读取并显示 `ro.product.odm.brand`、`ro.product.odm.manufacturer`、`ro.product.odm.model`
- 通过对比 `odm` 分区与 `vendor` 分区属性判断设备是否处于伪装状态
- 设备断开时 UI 显示未连接提示，操作按钮置灰

### FR-2: 设备信息伪装执行

- 点击 Start 前校验三个输入框是否全部填写，未填写则弹出提示要求补全
- 点击 Start 前校验输入值是否与当前设备信息一致，一致则弹窗提示无需伪装，不执行操作
- 执行顺序：adb root -> adb remount -> setenforce 0 -> pull build.prop -> 修改属性 -> push build.prop -> adb reboot
- 等待设备重启完成后（`sys.boot_completed=1`）执行验证
- 验证伪装后的属性值与预期一致
- 进度区域实时显示当前执行阶段

### FR-3: 设备信息重置

- 从 `vendor` 分区（`ro.product.vendor.*`）读取原始设备属性值
- 执行与伪装相同的 adb 流程将属性还原
- 重启后验证属性已恢复为原始值

### FR-4: 本地设备信息数据库

- 持久化存储设备信息记录，每条包含：brand、manufacturer、model、备注
- 唯一键为 brand+manufacturer+model 组合
- 支持完整 CRUD 操作：新增、查询、编辑、删除
- 数据存储在工具根目录下的本地文件中
- 支持从外部 JSON 文件导入设备档案，重复记录自动跳过

### FR-5: 输入框自动补全与下拉历史

- 三个输入框均支持下拉历史记录选择，点击下拉箭头可查看数据库中所有已保存的对应字段值
- 输入时实时过滤匹配的历史记录（大小写不敏感）
- 选取任一字段后，自动从数据库匹配并填充其余两个字段
- 多条匹配记录时按首字母排序，自动填入第一条
- 保存新档案或导入档案后，下拉列表实时刷新

### FR-6: 设备信息快捷选取弹窗

- 点击触发按钮后弹出固定大小弹窗
- 弹窗位置锚定在按钮右下角
- 左侧列表显示数据库中所有已保存的 model
- 鼠标悬停 model 上时，右侧区域实时显示对应备注内容
- 点击 model 后自动填充三个输入框并关闭弹窗
- 右键点击 model 可选择「编辑」或「删除」操作

### FR-7: 新设备信息保存对话框

- 点击 Start 时检测当前输入组合是否在数据库中
- 不存在时弹出保存对话框
- 对话框包含 brand、manufacturer、model（预填）和备注输入框
- 备注输入框置灰提示："请描述此设备信息对应哪些高帧游戏"
- Save 保存后继续执行伪装，Cancel 取消不执行

### FR-8: 错误处理

- ADB 操作失败时在进度区域打印具体错误信息并终止当前流程
- adb root 失败时提示："设备无 root 权限，请使用已 root 的设备"
- adb 环境不可用时提示："未检测到 adb 环境，请配置 adb 环境变量"
- 其他 adb 命令失败时显示具体错误原因

### FR-9: 设备档案导入

- 支持从 JSON 格式文件导入设备档案
- JSON 文件结构为 DeviceProfile 数组
- 导入时按 brand+manufacturer+model 组合去重，已有记录跳过
- 导入完成后反馈导入结果（成功条数、跳过条数）

### FR-10: 设置菜单

- 标题栏右侧放置齿轮图标按钮，取代原 Import 按钮
- 点击齿轮按钮弹出二级菜单，包含以下选项：
  - "Import Device Data"：触发设备档案导入流程（FR-9）
  - "Theme"：子菜单或切换项，在 Dark / Light 之间切换

### FR-11: 主题切换

- 支持暗色（Dark）和亮色（Light）两套主题
- 首次启动默认使用暗色主题
- 用户通过设置菜单切换主题，切换后立即生效，无需重启工具
- 主题偏好持久化保存到本地配置文件（`config.json`），下次启动自动恢复
- 两套主题的配色方案参见 `design/ui-design.md`

### FR-12: 打包与部署

- 工具打包为 Windows 可执行文件（.exe），使用 PyInstaller --onedir 模式
- 最终产物为 zip 压缩包，用户解压即用，无需安装 Python 环境
- 内置最小 ADB 集合（adb.exe + AdbWinApi.dll + AdbWinUsbApi.dll）
- ADB 查找优先级：系统 PATH 中的 adb > 工具内置 adb
- 若系统 PATH 已有 adb 环境，直接使用系统环境；若无，自动使用内置 adb

---

## Success Criteria

| 编号 | 标准                       | 度量                                              |
| ---- | -------------------------- | ------------------------------------------------- |
| SC-1 | 设备连接后自动显示设备信息 | 3 秒内完成显示                                    |
| SC-2 | 伪装操作端到端完成         | 用户从输入到验证成功全流程 < 2 分钟（含设备重启） |
| SC-3 | 重置操作端到端完成         | 用户点击 Reset 到验证成功 < 2 分钟（含设备重启）  |
| SC-4 | 伪装/重置成功率            | 100%（排除设备硬件故障）                          |
| SC-5 | 数据库记录持久化           | 工具关闭重启后历史记录不丢失                      |
| SC-6 | 自动补全响应               | 选取单项后 500ms 内完成其余字段填充               |
| SC-7 | 设备插拔自动响应           | 插入/拔出后 3 秒内自动刷新 UI 状态                |
| SC-8 | 错误提示准确性             | 每种错误类型均有对应的明确提示信息                |

---

## Key Entities

### DeviceProfile（设备档案）

| 字段         | 说明                                         |
| ------------ | -------------------------------------------- |
| brand        | 设备品牌（如 vivo、Xiaomi）                  |
| manufacturer | 设备制造商（如 vivo、Xiaomi Communications） |
| model        | 设备型号（如 V2505A）                        |
| notes        | 备注（如"支持120帧原神、王者荣耀"）          |

### DeviceState（设备状态）

| 字段                  | 说明                            |
| --------------------- | ------------------------------- |
| current_brand         | 当前 odm 分区 brand 值          |
| current_manufacturer  | 当前 odm 分区 manufacturer 值   |
| current_model         | 当前 odm 分区 model 值          |
| original_brand        | vendor 分区原始 brand 值        |
| original_manufacturer | vendor 分区原始 manufacturer 值 |
| original_model        | vendor 分区原始 model 值        |
| is_disguised          | 是否处于伪装状态                |
| is_connected          | 设备是否已连接                  |

---

## UI Layout

工具界面分为四行区域（详细设计参见 `design/ui-design.md`）：

**标题栏**: 显示工具名称，右侧放置齿轮（设置）按钮

1. **当前设备信息行**: 只读显示 brand / manufacturer / model + 伪装状态标签
2. **伪装设备信息行**: 快捷选取按钮 + brand / manufacturer / model 输入框（带下拉历史）
3. **进度信息区**: 终端风格区域，显示任务执行的实时进度文字和错误信息
4. **操作按钮行**: Start / Clear / Reset 三个按钮（设备未连接时置灰）

弹窗组件：

- **设置菜单**: 齿轮按钮点击后弹出，包含 "Import Device Data" 和 "Theme: Dark/Light" 选项
- **设备选取弹窗**: 左侧 model 列表 + 右侧备注悬浮显示 + 右键菜单支持编辑/删除
- **保存/编辑对话框**: brand / manufacturer / model / 备注 + Save / Cancel

主题：支持 Dark / Light 两套主题，默认暗色，用户偏好持久化保存

---

## Assumptions

1. 工具运行环境为 Windows PC（无需预装 Python 或 ADB）
2. 设备已开启 USB 调试，USB 线缆已连接
3. 设备支持 root 和 remount 操作
4. 设备型号信息存储在 `/odm/etc/build.prop` 中
5. 原始设备信息可从 `ro.product.vendor.*` 属性读取
6. 本地数据库使用 JSON 文件，无需额外安装
7. 导入文件格式为标准 JSON，结构与 DeviceProfile 实体一致
8. 工具以 zip 压缩包形式分发，内置 Python 运行时和最小 ADB 集合，解压即用

---

## Scope Boundaries

### In Scope

- 通过 ADB 读取/修改/还原设备 odm 属性（brand、manufacturer、model）
- 本地设备档案数据库的完整 CRUD 操作
- 从 JSON 文件导入设备档案
- 输入自动补全与快捷选取
- 执行进度实时展示
- 设备连接状态持续监听
- 错误处理与用户提示
- Dark / Light 双主题支持与切换
- 设置菜单（齿轮按钮，含导入和主题切换）
- PyInstaller 打包为 Windows exe（zip 分发）
- 内置 ADB 环境（优先使用系统 ADB）

### Out of Scope

- 多设备同时连接管理
- 修改 odm 以外分区的设备属性
- 网络同步或云端数据库
- 设备 root/remount 权限的自动获取（假设设备已具备）
- 设备档案导出功能

---

## Clarifications

### Session 2026-03-08

- Q1: ADB 操作失败时工具如何处理？ -> A: 进度区域打印报错信息并终止流程；若因无 root 权限导致失败，提示用户使用 root 版本设备；若因无 adb 环境导致失败，提示用户配置 adb 环境。
- Q2: 数据库中已保存的设备档案是否支持编辑和删除？ -> A: 支持完整 CRUD（新增、查询、编辑、删除）。
- Q3: 用户点击 Start 时输入框未全部填写如何处理？ -> A: 弹出提示，要求用户补全缺失字段。
- Q4: 工具是否需要持续监听设备连接状态？ -> A: 持续监听，设备插入/拔出时自动刷新第一行信息和按钮状态。
- Q5: 工具是否需要预置设备档案数据？ -> A: 提供可选的 JSON 格式导入文件，用户按需导入。
- Q6: 主题切换按钮放置位置？ -> A: 将标题栏右侧的 Import 按钮改为齿轮（设置）按钮，点击后弹出二级菜单，包含 "Import Device Data" 和 "主题切换" 两个选项。
- Q7: 内置 ADB 的范围？ -> A: 打包最小 ADB 集合（adb.exe + 必要 DLL），无需用户配置；若 PC 已有 adb 环境则优先使用系统环境。
- Q8: exe 打包方式？ -> A: 目录形式（PyInstaller --onedir）+ 压缩为 zip 分发，用户解压即用，启动速度快。
- Q9: 默认主题？ -> A: 暗色主题（Dark）。
- Q10: 主题偏好是否持久化？ -> A: 持久化保存到本地配置文件，下次启动自动恢复用户上次选择的主题。

### Implementation Changes (2026-03-08)

实现阶段的需求变更与 Bug 修复记录：

1. **[变更] FR-2 新增一致性检查**: 当伪装输入的 brand/manufacturer/model 与当前设备信息完全一致时，弹窗提示无需伪装，不执行操作
2. **[变更] FR-6 备注显示延迟取消**: 设备选取弹窗中的备注由「悬停 1 秒后显示」改为「悬停时实时显示」，提升操作效率
3. **[变更] FR-5 下拉历史记录**: ComboBox 下拉列表填充数据库中所有已保存值，保存/导入后实时刷新
4. **[修复] DevicePopup 父窗口异常**: 编辑/删除对话框的 parent 改为 MainWindow（而非 Popup），避免 Popup 失焦自动关闭导致卡死闪退
5. **[修复] QDialog 未导入**: device_popup.py 缺少 QDialog 导入导致 NameError 崩溃
6. **[修复] 暗色主题样式覆盖不全**: DevicePopup（QWidget + Popup 标志）未继承暗色背景，补充 `#devicePopup` QSS 样式和 `WA_StyledBackground` 属性
7. **[修复] QMessageBox 暗色主题**: 添加 QMessageBox 专用 QSS 样式，确保对话框文字在暗色模式下清晰可读
8. **[结构] 代码目录迁移**: 源代码和依赖从根目录迁移至 `source/` 子目录，设计文档和规范文件保留在根目录

---

## Dependencies

- ADB（Android Debug Bridge）命令行工具（内置于工具中，或使用系统已有环境）
- PyInstaller（构建时依赖，用于打包 exe）
- USB 连接的 Android 设备（已 root、已开启 USB 调试）
