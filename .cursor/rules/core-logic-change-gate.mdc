---
description: 核心逻辑修改管控规则：防止 Bug 修复时未经充分分析就修改核心算法导致回归
alwaysApply: true
---

# 核心逻辑修改管控

修改以下类型的代码时 MUST 先做影响分析，MUST NOT 直接"试着改改看"：

- 状态机逻辑（MonitorState 转换、触发条件）
- 检测算法（Jank 判定、丢帧计算、FPS 统计）
- 数据流路径（信号连接、跨线程通信）
- 数据结构变更（numpy 数组布局、聚合策略）

## 修改前 MUST 输出

```
## 核心逻辑变更分析
- 修改目标：[要解决什么问题]
- 影响方法：[列出所有受影响的方法和调用链]
- 风险点：[可能导致的副作用]
- 回退方案：[如果改坏了怎么恢复]
- 验证计划：[如何确认修复正确且无回归]
```

## 禁止行为

- MUST NOT 用瞬时值替代稳定基准值（如用瞬时 FPS 替代刷新率做基准）
- MUST NOT 在未评估上下游影响时修改公共方法签名
- MUST NOT 连续多次修改同一段核心逻辑而不跑测试验证
- MUST NOT 在 Bug 修复中引入超出修复范围的重构
