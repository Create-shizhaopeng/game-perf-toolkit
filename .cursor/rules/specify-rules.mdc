---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# lv-game-toolkit 开发速查

## 常用命令

```bash
python -m toolkit.app              # 启动 GUI
python scripts/run_all_tests.py    # 运行全部测试
python scripts/create_module.py <name>  # 创建新模块
```

## 不可违反的硬规则

1. 模块 MUST NOT 修改 `toolkit/` 目录
2. `service.py` MUST NOT 包含 GUI/CLI 代码
3. GUI 后台操作 MUST 使用 QThread + pyqtSignal
4. 所有输出 MUST 使用 UTF-8 编码
5. Pydantic 用于公共 API 的入参和返回值

## 版本号规范 (SemVer)

格式: `vMAJOR.MINOR.PATCH` — 通过 `git tag` 管理，构建脚本自动读取。

| 变更类型 | 版本位 | 示例 |
|---------|--------|------|
| Bug 修复、小调整 | PATCH +1 | v0.1.1 → v0.1.2 |
| 需求更新、功能迭代 | MINOR +1, PATCH 归零 | v0.1.2 → v0.2.0 |
| 新增模块、框架重构 | MAJOR +1, 其余归零 | v0.2.0 → v1.0.0 |

- 发版前 MUST 执行 `git tag vX.Y.Z` 并 `git push origin vX.Y.Z`
- 构建脚本通过 `git describe --tags` 自动提取版本号写入 `VERSION` 文件
- `toolkit/__version__` 优先从 `VERSION` 文件读取，开发环境从 git tag 读取
- MUST NOT 在代码中硬编码版本号

## 知识源指针（按需查阅，不要全量加载）

| 需要了解 | 查阅位置 |
|---------|---------|
| 架构原则与技术栈 | `.specify/memory/constitution.md` |
| 开发流程 | `.cursor/rules/spec-workflow.mdc` |
| 踩坑经验 | `doc/experience/development-pitfalls.md` |
| 模块开发指南 | `doc/knowledge/module-development-guide.md` |
| 特定模块约束 | `modules/<name>/AGENTS.md` |
| 项目知识库 | `doc/knowledge/README.md` |
| 跨项目通用知识 | `context/INDEX.md` |
| 架构设计 | `doc/architecture/architecture-overview.md` |
