---
description: Spec-Driven Development Workflow for Agent
globs: ["**/*"]
alwaysApply: true
---
# Spec-Driven Agent 工作流规范

本规则定义 Agent 在 spec-driven 开发中的必须遵循的闭环流程。
详见 `.specify/memory/constitution.md` § Spec-Driven 标准开发流程。

## 标准开发流程（主 spec / 子 spec 均适用）

| 步骤 | 动作 | 说明 |
|------|------|------|
| Step 1 | specify | 创建功能规格文档 |
| Step 2 | clarify | 需求澄清，与用户交互定好不清晰的需求 |
| Step 3 | UE/UI 设计 | 涉及界面时设计 2-3 种方案供用户选择，子模块风格与主模块一致 |
| Step 4 | plan | 制定实现计划 |
| Step 5 | tasks | 生成任务清单 |
| Step 6 | analysis | 需求/计划/任务一致性，FAIL 清零后方可实现 |
| Step 7 | implement | 执行实现 |
| Step 8 | analysis | 实现后验证，FAIL 清零 |

Step 3 仅在涉及 GUI/界面时必须执行。

## 用户验收 — 简单 Bug

不涉及功能变更、需求补充的简单修复：
1. spec task + Bug 描述 → 修复任务
2. spec implement → 修复
3. spec analysis → 确认一致性

## 用户验收 — 需求变更或设计调整

涉及需求补充、功能变更、设计调整：
1. spec clarify → 需求澄清
2. 涉及 UI 时 → 更新 UE/UI 设计
3. spec plan → 修复计划
4. spec task → 具体任务
5. spec analysis → clarify 与 task 一致性
6. spec implement → 执行修复
7. spec analysis（最终）→ 代码对齐 spec + constitution

## Bug 修复方法论

修复任何 Bug 时 MUST 遵循：
1. **分析根因**：通过日志、命令输出、代码追踪定位根本原因
2. **记录诊断**：在进度回调/日志中输出关键返回值和状态信息
3. **基于根因修复**：针对确认的根因编写修复代码，MUST NOT 盲目尝试
4. **验证修复**：通过测试和手动验证确认问题已解决
5. **更新文档**：涉及流程/逻辑变更时同步更新 spec 文档

## 通用约束

- 每次 analysis 必须 FAIL 项清零方可进入下一阶段
- 所有 clarify 决策必须回写到 spec.md 的 Clarifications 章节
- Constitution 是最高治理文档，实现不得违反其原则
- 新模块开发前 MUST 阅读 `scripts/doc/development-pitfalls.md` 避免重复踩坑
- 插件 context 键名 MUST 使用模块前缀命名空间（如 `dd_service`、`gp_adb`）
- GUI 后台线程 MUST 通过 `pyqtSignal` 通信，MUST NOT 直接操作 UI 控件
