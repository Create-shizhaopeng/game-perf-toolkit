---
description: Spec-Driven Development Workflow for Agent
globs: ["**/*"]
alwaysApply: true
---
# Spec-Driven Agent 工作流规范

完整流程和验收工作流详见 `.specify/memory/constitution.md`。本规则仅列出 Agent 必须遵循的核心约束。

## 开发流程速查

specify → clarify → [UE/UI] → plan → tasks → analysis → implement → analysis

- Step 3 (UE/UI) 仅在涉及 GUI 时执行
- 每次 analysis MUST FAIL 项清零方可进入下一阶段
- 所有 clarify 决策 MUST 回写 `spec.md` Clarifications 章节

## Bug 修复

- 简单 Bug：task → implement → analysis
- 需求变更：clarify → [UE/UI] → plan → task → analysis → implement → analysis
- MUST 先分析根因再修复，MUST NOT 盲目尝试
- 涉及流程/逻辑变更时 MUST 同步更新 spec 文档

## Speckit 路由

Speckit 仅在项目根级维护，模块级 Speckit 已移除（模块约束统一在 `AGENTS.md` 中管理）。

| 目标 | Speckit 路径 | Spec 输出目录 |
|------|-------------|-------------|
| toolkit 框架 | `.specify/` | `specs/` |
| 各模块 | 无独立 Speckit | `modules/<name>/specs/` |

模块开发时的约束和边界以 `modules/<name>/AGENTS.md` 为权威源。

## 硬约束

- Constitution（`.specify/memory/constitution.md`）是最高治理文档
- 新模块开发前 MUST 阅读 `doc/experience/development-pitfalls.md`
- 当前模块相关的 `AGENTS.md` MUST 在实现前阅读
- context 键名 MUST 使用模块前缀（如 `dd_service`、`gp_adb`）
- GUI 后台线程 MUST 通过 `pyqtSignal` 通信
