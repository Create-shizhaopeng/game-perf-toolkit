---
description: Spec-Driven Development Workflow for Agent
globs: ["**/*"]
alwaysApply: true
---
# Spec-Driven Agent 工作流规范

本规则定义 Agent 在 spec-driven 开发中的必须遵循的闭环流程。
详见 `.specify/memory/constitution.md` § Agent 实现与验收工作流。

## 实现后自检（必须）

Agent 完成 `speckit.implement` 后必须：
1. 自动执行 `spec analysis`
2. 确认 FAIL 项清零；若有 FAIL，修复后重新 analysis
3. WARN 项尽量修复，允许记录为已知限制后跳过

## 用户验收 — 简单 Bug

满足以下条件的反馈：不涉及功能变更、不涉及需求补充、属简单修复

处理流程：
1. `spec task` + Bug 描述 → 修复任务
2. `spec implement` → 修复
3. `spec analysis` → 确认一致性

## 用户验收 — 需求变更或设计调整

涉及需求补充、功能变更、设计调整的反馈

处理流程（按顺序执行）：
1. `spec clarify` → 需求澄清
2. `spec plan` → 修复计划
3. `spec task` → 具体任务
4. `spec analysis` → clarify 与 task 一致性
5. `spec implement` → 执行修复
6. `spec analysis`（最终）→ 代码对齐 spec + constitution

## 通用约束

- 每次 analysis 必须 FAIL 项清零方可进入下一阶段
- 所有 clarify 决策必须回写到 spec.md 的 Clarifications 章节
- Constitution 是最高治理文档，实现不得违反其原则
