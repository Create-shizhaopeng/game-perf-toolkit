---
description: Review Gate - Prevent implementation drift from spec
globs: ["**/*"]
alwaysApply: true
---
# 实现 Review 门禁

防止实现偏离预期，关键变更前 MUST 输出决策摘要供用户确认。

## 需要 Review 的场景

1. **架构决策**：涉及新文件创建、目录结构变更、跨模块交互变更
2. **接口变更**：修改 `service.py` 公共方法签名、修改 Pydantic 模型
3. **多文件变更**：单次修改涉及 3 个以上文件时
4. **偏离 spec**：实现方案与 spec/plan 描述不一致时

## Review 输出格式

在执行上述场景的变更前，MUST 先输出：

```
## 变更摘要
- 变更类型：[新增/修改/删除/重构]
- 影响范围：[涉及的文件/模块列表]
- 与 spec 的对齐：[一致/有偏离 — 偏离原因]
- 关键决策：[列出需要用户确认的设计选择]
```

## 自检清单

实现完成后 SHOULD 自检：
- [ ] 变更是否与 spec.md 中的功能需求一致
- [ ] 是否遵循了当前模块 AGENTS.md 的边界约束
- [ ] 是否有新的踩坑经验需要记录
- [ ] 是否有文档需要同步更新
